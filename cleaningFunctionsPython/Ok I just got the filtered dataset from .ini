Ok I just got the filtered dataset from jaylen and uploaded it to the “altered-datasets” folder in the GitHub where you put the merged datasets, this file is called “all_chick_data_final_filter.csv”

And based on that I prepared the updated tasks for you:

    ⁃    Use “all_chick_data_final_filter.csv”
    ⁃    Delete “date_parsed” column (it is the same as “datetime”)
    ⁃    Change month column so that the values are actually the names of the month (i.e. where the column says 1 it should be January, 2 = February, etc…)
    ⁃    Change day column so instead of the day of the month it is day of the year
    ⁃    i.e. 2014-06-26 is the 177th day of the year
    ⁃    Add cohort column for the bird (i.e. year it fledged)
    ⁃    Rename “start date” column to “fledge date”
    ⁃    Add months_post_fledge column
    ⁃    Use datetime - fledge date for the calculation
    ⁃    Values of column should be numerical values (i.e. if datetime - fledge_date is less than 1 month old then result should be 0, if the subtraction is between something like 2009-25-07 - 2009-18-06, then there is just over a 1 month difference but not 2 whole months —> row value should be 1)
    ⁃    Add years_post_fledge column (binned)
    ⁃    0-12 months = “year 1” (post-fledging dispersal)
    ⁃    12-36 months = “year 2-3” (immature roaming phase)
    ⁃    36-60 months = “year 4-5” (pre-breeding prospecting)
    ⁃    Add “distance_from_colony” column
    ⁃    You already did teh step_distance one between consecutive datapoints so instead just compare current distance to colony start distance now